# 04 — Pub/Sub source: APA 7 citation + verified notes (research)

**What to build:** The authoritative Google Cloud Pub/Sub documentation is located and its
relevant features verified, so the Phase 3 report can cite it. Produce a notes file capturing
the verified facts about ordering keys, redelivery / retry policy (exponential backoff), and
dead-letter topics, each tied to its specific doc source, plus a correct APA 7 web citation.

Context: the 5th source added for the Phase 3 managed-broker scaling framing
(`wayfinder/tickets/04-phase3-report-outline.md`). Same rigor as the four PDF notes already
in `resources/notes/` — facts limited to what the docs state, no outside claims. Independent
of all code work; safe to run in parallel.

**Blocked by:** None — can start immediately.

**Status:** done

- [x] `resources/notes/` file for GCP Pub/Sub with verified facts (ordering keys, redelivery/retry, dead-letter topics), each attributed to a specific doc page/URL
- [x] A correct APA 7 web citation for the source(s)
- [x] Facts limited to what the docs state, matching the rigor of the existing paper notes

**When done:** tick the acceptance criteria above and set `Status: done`.
